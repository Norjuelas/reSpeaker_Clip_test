# Test/factory firmware — flashed directly via J-Link.
# No MCUboot, no b0n secure-boot, no netcore (clean single-image build).
SB_CONFIG_BOOTLOADER_NONE=y
SB_CONFIG_SECURE_BOOT_NETCORE=n
SB_CONFIG_NETCORE_NONE=y
