# OBSOLETO desde 2026-08-18 — su contenido vive ahora en prj.conf.
#
# Este overlay encendia TLS y apagaba BLE y el modo AP. Se pasaba a mano con
# -DEXTRA_CONF_FILE, y esa era exactamente la fragilidad: build-prod y
# build-relcheck-prod se compilaron sin el, o sea SIN TLS y CON las dos radios
# abiertas, mientras la documentacion de seguridad decia lo contrario.
#
# La postura segura ya es la que sale de `west build` sin argumentos extra.
# Pasarlo ahora no rompe nada (queda vacio), pero no hace falta.
#
# Para desarrollo con BLE o modo AP:
#   west build ... -- -DEXTRA_CONF_FILE=$(pwd)/applications/clip/overlay-dev-radio.conf
